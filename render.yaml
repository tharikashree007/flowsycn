services:
  # Backend API Service
  - type: web
    name: flowsync-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://Tharika:tharika13@cluster0.j0wvduu.mongodb.net/project_management?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    plan: free

  # Frontend Static Site
  - type: static
    name: flowsync-frontend
    buildCommand: cd "project management system" && npm install && npm run build
    staticPublishPath: project management system/dist
    plan: free
