services:
  # Full-stack service (Frontend + Backend)
  - type: web
    name: flowsync-app
    env: node
    buildCommand: cd "project management system" && npm install && npm run build && cd .. && node copy-build.js && cd backend && npm install
    startCommand: npm run start
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://Tharika:tharika13@cluster0.j0wvduu.mongodb.net/project_management?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    plan: free
