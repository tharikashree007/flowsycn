# MongoDB Atlas Connection String
# IMPORTANT: For Render deployment, set MONGODB_URI as an Environment Variable in Render dashboard
# Do not commit your actual MongoDB Atlas credentials to version control!
#
# Local Development: Replace the value below with your MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
#
# Render Deployment: Add MONGODB_URI environment variable in Render with your Atlas connection string
# MongoDB Atlas connection - replace with your actual connection string
MONGODB_URI=mongodb+srv://Tharika:tharika13@cluster0.j0wvduu.mongodb.net/project_management?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret for authentication
# IMPORTANT: For production, use a strong random string (at least 32 characters)
# In Render, set JWT_SECRET as an Environment Variable (Render can auto-generate this)
JWT_SECRET=your_super_secure_jwt_secret_key_at_least_32_characters_long

# Server Port
# In production (Render), this will be overridden by the PORT environment variable
PORT=5000
